// Mirrors the global-using set relied on by the moved tests in their prior home
// (Farm.Web.Api.Tests/Usings.cs, issue #2033's Farm.Infrastructure.Tests/GlobalUsings.cs) so this
// relocation (issue #2034) does not require touching every individual file's using directives.
global using FluentAssertions;
global using Xunit;
