# Example environment configuration# Example environment configuration

# Copy this file to .env.development.local for local development# Copy this file to .env.development.local for local development



# SignalR URL for development - connects directly to API server# SignalR URL for development - connects directly to API server

# Uncomment and set this for development:# VITE_SIGNALR_URL=http://localhost:5245/hubs/printers

# VITE_SIGNALR_URL=http://localhost:5245/hubs/printers

# API Base URL (optional, defaults to relative URLs which work via proxy)

# API Base URL (optional, defaults to relative URLs which work via proxy)# VITE_API_BASE_URL=http://localhost:5245
# VITE_API_BASE_URL=http://localhost:5245