{
  "_comment": "Coverage floor only: every direct test, helper, imported source, or global TypeScript diagnostic is fatal. minimumTestFileCount counts real .test.ts/.test.tsx files, excluding helpers, duplicate realpaths and @ts-nocheck files. Preserve this measured floor when changing project coverage.",
  "minimumTestFileCount": 532
}
