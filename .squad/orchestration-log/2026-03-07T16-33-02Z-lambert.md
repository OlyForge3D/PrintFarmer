# Lambert — Bug Fix Session (2026-03-07T16:33:02Z)

**Agent ID:** agent-6  
**Model:** claude-sonnet-4.5  
**Mode:** background  
**Session Trigger:** Code review of feature/sprint-3-locations-dispatch-docs (18 commits)  
**Status:** ✅ SUCCESS

## Work Summary

**Task:** Fix 2 HIGH-severity bugs in BatchDispatchService discovered during code review.

### Bug 1: N+1 Query in DispatchLeastBusyAsync (HIGH)

**Problem:** Queue depth DB query executed inside foreach loop over jobs — dispatching N jobs triggered N separate DB round-trips.

**Fix:** Hoisted the `_appDbContext.PrintJobs.Where(...).Count()` query outside the loop. In-batch queue-depth adjustments still work correctly via `batchAssignments` dictionary tracking.

**Impact:** Batch dispatch of 50 jobs now executes 1 queue-depth query instead of 50.

**File:** `src/infra/Services/Queue/Dispatch/BatchDispatchService.cs` (~line 88)

### Bug 2: Divide-by-Zero in Average Score (MEDIUM)

**Problem:** `GetQueueStatusAsync()` called `.Average()` on dispatch logs filtered by `.Where(l => l.Score.HasValue)` without guarding against empty sequence. If all recent dispatches have null scores, throws `InvalidOperationException`.

**Fix:** Used `.DefaultIfEmpty(0).Average()` to safely return 0 when no scored logs exist.

**Impact:** `GET /api/dispatch/queue-status` endpoint no longer throws on endpoints with null-score dispatch history.

**File:** `src/infra/Services/Queue/Dispatch/BatchDispatchService.cs` (~line 165)

## Validation

- ✅ Build: 0 errors, 0 new warnings
- ✅ Tests: 1572/1572 API tests passing (0 failures)
- ✅ Code follows existing patterns (LINQ, nullable handling)
- ✅ No breaking changes to public APIs

## Commit

**SHA:** 3806a374  
**Message:** `fix: resolve N+1 query, divide-by-zero, and missing TS field in dispatch`  
**Files Changed:** 2 (BatchDispatchService.cs, DispatchSettingsPanel.tsx)

## Key Learnings

1. **N+1 in batch loops:** Any DB query inside a loop over items being processed should trigger a code review flag. Hoist before the loop; track in-memory adjustments for within-batch state.

2. **`.Average()` on empty sequences:** Always use `.DefaultIfEmpty(fallback).Average()` or check `.Any()` before calling `.Average()` on LINQ results that could be empty after filtering.

## Next Steps

- None; bugs fixed and tested
- PR review: verify load-balancing strategy integration (Ripley's TypeScript fix)
